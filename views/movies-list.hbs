<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Movies</title>
</head>
<body>
  <h1>Movies</h1>
  <table>
    <thead>
      <tr>
        <th>Title</th>
        <th>Description</th>
        <th>Critic Rating</th>
        <th>My Rating</th>
        <th>Image</th>
      </tr>
    </thead>
    <tbody>
      {{#each movies}}
      <tr>
        <td>{{this.title}}</td>
        <td>{{this.description}}</td>
        <td>{{this.criticRating}}</td>
        <td>{{this.myRating}}</td>
        <td><img src="{{this.imageUrl}}" alt="{{this.title}}" width="100"></td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  <a href="/">Add New Movie</a>
</body>
</html>